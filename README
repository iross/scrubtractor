```
docker stop scrub; docker build --rm=true -t scrubtractor .; docker run -v $(pwd)/input:/input -v $(pwd)/output:/output --rm --name scrub -dt scrubtractor;


# inside container

python3 Document.py
```

