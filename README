```
docker stop scrub; docker build --rm=true -t scrubtractor .; docker run -v $(pwd)/input:/home/work/input -v $(pwd)/output:/home/work/output --rm --name scrub -dt scrubtractor;


# inside container

python3 Document.py
```

